[project]
name = "container-environment"
version = "0.0.0"
description = "Dependency-only Python environment for Nextflow tasks"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = [
    "prek>=0.3.9,<0.4",
]
